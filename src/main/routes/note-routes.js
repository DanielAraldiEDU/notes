/**
 * @param {*} router - The router instance to use for the routes.
 */
export function routes(router) {
  router.get('/notes', () => {});
  router.post('/note', () => {});
  router.put('/note', () => {});
  router.delete('/note', () => {});
}
